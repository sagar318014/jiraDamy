
@model ViewModel.LableListViewModel

@{
    ViewBag.Title = "addLable";
    Layout = "~/Views/Shared/_AuthorizedLayout.cshtml";
}
@section Grid{

   

    @using (Html.BeginForm("AddLable", "Home", FormMethod.Post))
    {
        <lebel>Label Name</lebel>
        <div>
            @Html.TextBoxFor(x => x.LableName, new { id = "lableid" })
            @Html.ValidationMessageFor(x => x.LableName)
            <button type="submit" class="btn btn-success" >Add</button>

        </div>

    }


}

